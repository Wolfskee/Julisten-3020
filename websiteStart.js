gsap.from("#logo", {
    duration: 0.75,      // Duration of the animation
    scale: 0.5,       // Start scale
    ease: "bounce.out", // Easing function for the bounce effect
    opacity: 0,       // Start with an opacity of 0
    y: -100           // Start 100px above its final position
});

